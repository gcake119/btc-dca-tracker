# 資料格式規範

> btc-dca-tracker 資料交換格式完整說明

**版本**：v1.0.0  
**最後更新**：2026-01-19

---

## 📋 目錄

- [概覽](#概覽)
- [CSV 格式](#csv-格式)
- [JSON 格式](#json-格式)
- [欄位說明](#欄位說明)
- [資料驗證規則](#資料驗證規則)

---

## 概覽

本系統支援兩種資料格式：

| 格式 | 用途 | 特性 |
|------|------|------|
| **JSON** | 內部存儲、完整備份 | 包含 metadata、支援複雜結構 |
| **CSV** | 資料匯入/匯出、Excel 編輯 | 易於人工編輯、相容性高 |

---

## CSV 格式

### 標準 CSV 範本

```csv
date,exchange,pair,side,baseAsset,quoteAsset,baseAmount,quoteAmount,price,feeAsset,feeAmount,notes
2025-01-10,Binance,ADA/BTC,BUY,ADA,BTC,100,0.005,0.00005,ADA,0.1,First DCA
2025-01-15,OKX,BTC/USDT,BUY,BTC,USDT,0.5,21500,43000,USDT,10.75,Monthly DCA
2025-02-01,Kraken,ETH/BTC,BUY,ETH,BTC,2,0.04,0.02,ETH,0.002,Altcoin rotation
```

### CSV 欄位順序

| 順序 | 欄位名稱 | 必填 | 說明 |
|------|---------|------|------|
| 1 | `date` | ✅ | 交易日期（YYYY-MM-DD） |
| 2 | `exchange` | ✅ | 交易所名稱 |
| 3 | `pair` | ✅ | 交易對（BASE/QUOTE） |
| 4 | `side` | ✅ | 買賣方向（BUY/SELL） |
| 5 | `baseAsset` | ✅ | 基礎資產 |
| 6 | `quoteAsset` | ✅ | 計價資產 |
| 7 | `baseAmount` | ✅ | 基礎資產數量 |
| 8 | `quoteAmount` | ✅ | 計價資產數量 |
| 9 | `price` | ❌ | 單價（可自動計算） |
| 10 | `feeAsset` | ❌ | 手續費幣種 |
| 11 | `feeAmount` | ❌ | 手續費數量 |
| 12 | `notes` | ❌ | 備註說明 |

### CSV 編輯建議

**Excel / Google Sheets：**
1. 開啟 CSV 檔案時選擇「UTF-8 編碼」
2. 日期格式設為文字，避免自動轉換
3. 儲存時選擇「CSV UTF-8（逗號分隔）」

**注意事項：**
- ✅ 欄位有逗號時，必須用雙引號包圍：`"Monthly DCA, aggressive"`
- ✅ 數字欄位不要加千分位符號
- ✅ 日期統一使用 `YYYY-MM-DD` 格式
- ❌ 避免使用 Excel 的日期自動格式化

---

## JSON 格式

### 完整 JSON 結構

```json
{
  "metadata": {
    "version": "1.0.0",
    "userId": "user@example.com",
    "exportDate": "2026-01-19T10:30:00Z",
    "lastModified": "2026-01-19T10:30:00Z"
  },
  "trades": [
    {
      "id": "trade_1704873600000",
      "date": "2025-01-10",
      "timestamp": 1704873600000,
      "exchange": "Binance",
      "pair": "ADA/BTC",
      "side": "BUY",
      "baseAsset": "ADA",
      "quoteAsset": "BTC",
      "baseAmount": 100,
      "quoteAmount": 0.005,
      "price": 0.00005,
      "feeAsset": "ADA",
      "feeAmount": 0.1,
      "notes": "First DCA"
    },
    {
      "id": "trade_1705449600000",
      "date": "2025-01-15",
      "timestamp": 1705449600000,
      "exchange": "OKX",
      "pair": "BTC/USDT",
      "side": "BUY",
      "baseAsset": "BTC",
      "quoteAsset": "USDT",
      "baseAmount": 0.5,
      "quoteAmount": 21500,
      "price": 43000,
      "feeAsset": "USDT",
      "feeAmount": 10.75,
      "notes": "Monthly DCA"
    }
  ],
  "wallets": []
}
```

### JSON 結構說明

#### Metadata 物件

| 欄位 | 類型 | 必填 | 說明 |
|------|------|------|------|
| `version` | string | ✅ | 資料格式版本（語意化版本） |
| `userId` | string | ✅ | 使用者識別（email 格式） |
| `exportDate` | string | ✅ | 匯出時間（ISO 8601） |
| `lastModified` | string | ✅ | 最後修改時間（ISO 8601） |

#### Trade 物件

| 欄位 | 類型 | 必填 | 說明 |
|------|------|------|------|
| `id` | string | ✅ | 交易唯一 ID（`trade_` + timestamp） |
| `date` | string | ✅ | 交易日期（YYYY-MM-DD） |
| `timestamp` | number | ✅ | Unix timestamp（毫秒） |
| `exchange` | string | ✅ | 交易所名稱 |
| `pair` | string | ✅ | 交易對（BASE/QUOTE） |
| `side` | string | ✅ | 買賣方向（BUY/SELL） |
| `baseAsset` | string | ✅ | 基礎資產符號 |
| `quoteAsset` | string | ✅ | 計價資產符號 |
| `baseAmount` | number | ✅ | 基礎資產數量 |
| `quoteAmount` | number | ✅ | 計價資產數量 |
| `price` | number | ❌ | 單價（可自動計算） |
| `feeAsset` | string | ❌ | 手續費幣種 |
| `feeAmount` | number | ❌ | 手續費數量 |
| `notes` | string | ❌ | 備註說明 |

---

## 欄位說明

### 必填欄位

#### date（交易日期）
- **格式**：`YYYY-MM-DD`
- **範例**：`2025-01-10`
- **驗證**：必須是有效日期，不可為未來日期

#### exchange（交易所）
- **格式**：字串
- **範例**：`Binance`, `OKX`, `Kraken`, `Coinbase`
- **建議**：使用交易所官方名稱

#### pair（交易對）
- **格式**：`BASE/QUOTE`
- **範例**：`BTC/USDT`, `ADA/BTC`, `ETH/BTC`
- **規則**：必須包含斜線分隔兩個資產

#### side（買賣方向）
- **格式**：`BUY` 或 `SELL`
- **範例**：`BUY`（買入）、`SELL`（賣出）
- **驗證**：僅接受大寫

#### baseAsset（基礎資產）
- **格式**：資產符號（大寫）
- **範例**：`BTC`, `ETH`, `ADA`, `SOL`
- **規則**：必須與 `pair` 中的 BASE 一致

#### quoteAsset（計價資產）
- **格式**：資產符號（大寫）
- **範例**：`USDT`, `BTC`, `USD`, `EUR`
- **規則**：必須與 `pair` 中的 QUOTE 一致

#### baseAmount（基礎資產數量）
- **格式**：正數（最多 8 位小數）
- **範例**：`0.5`, `100`, `1.23456789`
- **驗證**：必須 > 0

#### quoteAmount（計價資產數量）
- **格式**：正數（最多 8 位小數）
- **範例**：`21500`, `0.005`, `430.5`
- **驗證**：必須 > 0

### 選填欄位

#### price（單價）
- **格式**：正數
- **計算**：`quoteAmount / baseAmount`
- **說明**：若未提供，系統自動計算

#### feeAsset（手續費幣種）
- **格式**：資產符號
- **範例**：`USDT`, `BTC`, `BNB`
- **說明**：通常與 baseAsset 或 quoteAsset 相同

#### feeAmount（手續費數量）
- **格式**：非負數
- **範例**：`0.1`, `10.75`
- **說明**：可為 0

#### notes（備註）
- **格式**：字串（最多 500 字元）
- **範例**：`Monthly DCA`, `Altcoin rotation strategy`
- **說明**：自由文字欄位

---

## 資料驗證規則

### 基本驗證

| 規則 | 說明 |
|------|------|
| **日期有效性** | date 必須是有效日期，不可為未來 |
| **數量正數** | baseAmount, quoteAmount 必須 > 0 |
| **交易對一致性** | pair 必須符合 `baseAsset/quoteAsset` |
| **買賣方向** | side 僅接受 `BUY` 或 `SELL` |
| **資產符號格式** | 僅接受大寫字母，2-10 字元 |

### 進階驗證

| 規則 | 說明 |
|------|------|
| **價格計算** | price ≈ quoteAmount / baseAmount（允許 0.01% 誤差） |
| **手續費合理性** | feeAmount 不應超過 baseAmount 或 quoteAmount 的 5% |
| **重複交易檢查** | 相同 timestamp + exchange + pair 視為重複 |

### 匯入時錯誤處理

**驗證失敗時：**
1. 顯示詳細錯誤訊息（行號、欄位名稱、錯誤原因）
2. 可選擇跳過錯誤行或停止匯入
3. 提供錯誤報告下載

**範例錯誤訊息：**
```
第 3 行錯誤：baseAmount 必須大於 0（當前值：-10）
第 5 行錯誤：date 格式不正確（當前值：01/15/2025，應為：2025-01-15）
第 7 行錯誤：side 僅接受 BUY 或 SELL（當前值：buy）
```

---

## 範例檔案

### 最小 CSV 範例

```csv
date,exchange,pair,side,baseAsset,quoteAsset,baseAmount,quoteAmount
2025-01-10,Binance,BTC/USDT,BUY,BTC,USDT,0.01,430
```

### 完整 CSV 範例

```csv
date,exchange,pair,side,baseAsset,quoteAsset,baseAmount,quoteAmount,price,feeAsset,feeAmount,notes
2025-01-10,Binance,ADA/BTC,BUY,ADA,BTC,100,0.005,0.00005,ADA,0.1,"First DCA, conservative"
2025-01-15,OKX,BTC/USDT,BUY,BTC,USDT,0.5,21500,43000,USDT,10.75,Monthly DCA
2025-02-01,Kraken,ETH/BTC,BUY,ETH,BTC,2,0.04,0.02,ETH,0.002,Altcoin rotation
```

### 空白 JSON 範本

```json
{
  "metadata": {
    "version": "1.0.0",
    "userId": "",
    "exportDate": "",
    "lastModified": ""
  },
  "trades": [],
  "wallets": []
}
```

---

## 版本歷史

| 版本 | 日期 | 變更說明 |
|------|------|----------|
| v1.0.0 | 2026-01-19 | 初始版本，定義 CSV/JSON 格式規範 |

---

## 相關文件

- [API.md](./API.md) - API 端點與資料交換格式
- [SPECIFICATION.md](../spec/SPECIFICATION.md) - 完整系統規格書
- [README.md](../README.md) - 專案說明
